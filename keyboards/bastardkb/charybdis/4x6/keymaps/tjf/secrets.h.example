/* Copy to secrets.h (git ignores it) and fill in.  Used by the M_PASS,
 * M_USER and M_EMAIL keys in keymap.c; any left empty do nothing. */
#pragma once

#define SECRET_PASSWORD ""
#define SECRET_USERNAME ""
#define SECRET_EMAIL ""
